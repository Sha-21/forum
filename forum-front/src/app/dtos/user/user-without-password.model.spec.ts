import { UserWithoutPassword } from './user-without-password.model';

describe('UserWithoutPassword', () => {
  it('should create an instance', () => {
    expect(new UserWithoutPassword()).toBeTruthy();
  });
});
